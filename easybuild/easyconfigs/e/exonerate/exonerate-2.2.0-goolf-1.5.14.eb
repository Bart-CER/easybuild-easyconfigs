name = "exonerate" # tweaked by EasyBuild (was: 'NAME')
version = "2.2.0" # tweaked by EasyBuild (was: 'VERSION')
homepage = "http://www.ebi.ac.uk/~guy/exonerate/" # tweaked by EasyBuild (was: 'http://www.example.com')
description = """Generic tool for pairwise sequence comparison"""

# toolchain name should be 'TEMPLATE' if this is a template, so EasyBuild knows and is willing to use it as a template
toolchain = {'version': '1.5.14', 'name': 'goolf'} # tweaked by EasyBuild (was: {'name': 'TEMPLATE', 'version': 'TK_VERSION'})
toolchainopts = {}  # toolchain options, e.g. opt, pic, usempi, optarch, ...
source_urls = ["http://www.ebi.ac.uk/~guy/exonerate/"] # tweaked by EasyBuild (was: ['http://www.example.com'])
sources = ["exonerate-2.2.0.tar.gz"] # tweaked by EasyBuild (was: ['%s-%s.tar.gz' % (name, version)])

patches = []

dependencies = []

sanity_check_paths = {
                      'files': [],
                      'dirs': ["."]
                     }

moduleclass = 'bio'
